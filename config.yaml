# config.yaml
experiment:
  meta: true
  batch_size: 1024
  train_epoch: 400
  learning_rate: 0.01
  weight_decay: 0.0025
  scheduler: "cosine"

  meta_learning_rate: 0.01
  n_tasks_per_meta_batch: 20 # Subjects for maml
  inner_steps: 5
  n_folds: 54 #LOSO
  from_to_folds: [47, 54] # from 44 to 4
  kfold: 3 #CV
  gpu: 0
  log_timing: false
  run_after_early_stop: false
  
  datapath: "data/preprocessed_KU_eeg/KU_mi_smt.h5"
  outpath: "results/" # saved to subfolders
  tensorboard_path: "tensorboard_logs"
